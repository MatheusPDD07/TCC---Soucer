import { Story, Meta } from '@storybook/react'
import DocumentsTable from '.'

export default {
  title: 'DocumentsTable',
  component: DocumentsTable
} as Meta

export const Default: Story = () => <DocumentsTable />
